tiers:
  - name: "0"
  - name: "1"

object_store_backends:
  - backend_type: file
    tier_names: ["0"]
    config: 
      root: phobos_tests_object_store
  - backend_type: mock_phobos
    tier_names: ["1"]

event_feed:
  active: true