namespace regression {
    
    ///
    /// @brief name with pointers
    ///
    /// If the pointer arguments in a function appear at unfavorable places, they 
    /// get converted into italic text. This should not be the case!
    ///
    /// @param a first pointer argument
    /// @param b second pointer argument
    ///
    void function_with_pointers(int *a, int* b);
}