namespace regression {
    
    ///
    /// @brief spaceless templates
    ///
    /// If Template parameters don't have spaces between the <> and their name,
    /// they are interpreted as html nodes in names and the <> disappear
    ///
    /// @param a templated argument
    ///
    void function_with_spaceless_templates(std::vector<std::string> a);
}